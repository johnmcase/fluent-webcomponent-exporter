import {
  DireflowComponent
} from 'direflow-component';
import SliderComponent from './SliderComponent';

export default DireflowComponent.create({
  component: SliderComponent,
  configuration: {
    tagname: 'symplr-slider',
    // useShadow: false
  }
});
